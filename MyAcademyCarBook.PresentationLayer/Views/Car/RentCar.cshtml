@model List<Car>
@{
    ViewData["Title"] = "RentCar";
    Layout = "~/Views/UILayout/Index.cshtml";
}
<div class="site-section bg-light">
    <div class="container">
        <h2>İstediğiniz Kriterlere Göre Toplam @Model.Count Araç Listelendi.</h2>
        <div class="row">
            @if (Model.Any())
            {

                @foreach (var item in Model)
                {
                    
                        
                   
                    <div class="col-lg-4 col-md-6 mb-4">
                        <div class="item-1">
                        <a href="#"><img src="@item.ImageUrl" alt="Image" class="img-fluid" style="height:210px; width:350px;"></a>
                        <div class="item-1-contents">
                            <div class="text-center">
                                <h3><a href="#">@item.Brand.BrandName @item.Model</a></h3>

                                
                            </div>
                            <ul class="specs">
                                <li>
                                    <span>Model Yılı</span>
                                    <span class="spec">@item.Year</span>
                                </li>
                                <li>
                                    <span>Kişi Sayısı</span>
                                    <span class="spec">@item.PersonCount</span>
                                </li>
                                <li>
                                    <span>Vites Türü</span>
                                    <span class="spec">@item.GearType</span>
                                </li>
                                <li>
                                    <span>Km</span>
                                    <span class="spec">@item.Km</span>
                                </li>
                                <li>
                                    <span>Vites Türü</span>
                                    <span class="spec">@item.GearType</span>
                                </li>
                               
                            </ul>
                            <div class="d-flex action">
                                <a href="/Car/CarList" class="btn btn-primary">Hemen Kirala</a>
                                <a href="/Car/CarDetail/@item.CarID" class="btn btn-warning" style="margin-left:1.2rem">Araç Detayları</a>
                            </div>
                        </div>
                    </div>
                </div>
                }
            }
            else
            {
                <div class="col-12 text-center">
                    <h2>İstediğin Kriterlere Göre Araçımız Yoktur</h2>
                </div>
            }
        </div>
    </div>
</div>
<div>
</div>




